#!/usr/bin/node

// Create an array containing the three required language strings
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through each element of the array using its length
for (let i = 0; i < languages.length; i++) {
  // Print each language string using a single console.log call
  console.log(languages[i]);
}
