#!/usr/bin/node

// Define an object with type and initial value properties
const myObject = {
  type: 'object',
  value: 12
};

// Print the initial state of the object
console.log(myObject);

// Update the value property of the object to 89
myObject.value = 89;

// Print the modified object showing the updated value
console.log(myObject);
